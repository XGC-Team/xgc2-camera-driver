name: ci

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  compliance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate product contracts
        run: .xgc2/scripts/check_package_compliance.sh
  build-debs:
    strategy:
      fail-fast: false
      matrix:
        include:
          - {architecture: amd64, runner: ubuntu-latest, distribution: bionic, image: ghcr.io/xgc-team/xgc2-images/xgc2-build-bionic-full-melodic:1.0.0}
          - {architecture: arm64, runner: ubuntu-24.04-arm, distribution: bionic, image: ghcr.io/xgc-team/xgc2-images/xgc2-build-bionic-full-melodic:1.0.0}
          - {architecture: amd64, runner: ubuntu-latest, distribution: focal, image: ghcr.io/xgc-team/xgc2-images/xgc2-build-focal-full-noetic:1.0.0}
          - {architecture: arm64, runner: ubuntu-24.04-arm, distribution: focal, image: ghcr.io/xgc-team/xgc2-images/xgc2-build-focal-full-noetic:1.0.0}
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - name: Prepare host CI directories
        run: install -d "$GITHUB_WORKSPACE/.ci/build-manifests"
      - name: Build, test, and install-check camera driver package
        run: |
          .xgc2/scripts/build_debs_in_docker.sh \
            --image "${{ matrix.image }}" \
            --work-dir "$RUNNER_TEMP/xgc2-camera-ros1-${{ matrix.distribution }}-${{ matrix.architecture }}" \
            --output-dir "$GITHUB_WORKSPACE/debs"
      - name: Create trusted artifact manifest
        run: |
          version="$(sed -nE 's/^version:[[:space:]]*([^[:space:]#]+).*/\1/p' .xgc2/product.yml | head -n1)"
          python3 .xgc2/scripts/xgc2_artifact_manifest.py build \
            --deb-dir debs --output-dir .ci/build-manifests \
            --product xgc2-camera-ros1 --product-version "$version" \
            --distribution "${{ matrix.distribution }}" --architecture "${{ matrix.architecture }}" \
            --source-sha "$GITHUB_SHA" --ci-run-id "$GITHUB_RUN_ID" \
            --ci-workflow "$GITHUB_WORKFLOW" --ci-workflow-ref "$GITHUB_WORKFLOW_REF"
      - uses: actions/upload-artifact@v4
        with:
          name: xgc2-camera-ros1-debs-${{ matrix.distribution }}-${{ matrix.architecture }}
          path: |
            debs/*.deb
            .ci/build-manifests/*.json
          include-hidden-files: true
          retention-days: 14


